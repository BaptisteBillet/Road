using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class PanelSelection : MonoBehaviour {

	

	// Use this for initialization
	void Start()
	{
		/*
		m_BlockMax = m_BlockListNavMesh.Count;
		int i = 0;

		foreach(GameObject go in m_BlockListNavMesh)
		{
			m_BlockInstance = Instantiate(m_BlockListNavMesh[i]);
			m_BlockListPanel.Add(m_BlockInstance);
			m_BlockListPanel[i].transform.position = new Vector3(x + (decalx * i), y + (decaly * i), z);
			m_BlockListPanel[i].transform.parent = this.gameObject.transform;
			i++;
		}
		i = 0;
		


	}
	
	// Update is called once per frame
	void Update () {
	
	}*/
	}
}
